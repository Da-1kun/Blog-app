<div class="contactForm">
  <%= form_for @user do |f| %>
    <%= render 'shared/errors', obj: @user %> 
    <div class="row100">
      <div class="inputBx100">
        <%= f.text_field :username, placeholder: "Username", autocomplete: 'off' %>
      </div>
    </div>
    <div class="row100">
      <div class="inputBx100">
        <%= f.email_field :email, placeholder: "E-mail address", autocomplete: 'off' %>
      </div>
    </div>
    <div class="row100">
      <div class="inputBx100">
        <%= f.password_field :password, placeholder: "Password", autocomplete: 'off' %>
      </div>
    </div>
    <div class="row100">
      <div class="inputBx100">
        <%= f.submit(@user.new_record? ? "create account" : "Update account") %>
      </div>
    </div>
  <% end %>
</div>